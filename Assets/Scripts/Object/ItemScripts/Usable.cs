using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 소비 아이템을 정의합니다.
// 기본적으로 아이템 개수를 가지며, 개수는 플레이어가 게임 내에서 증가 혹은 감소시킬수 있습니다.
// 해당 스크립트는 하위 오브젝트를 위한 상위 클래스로, 소비 아이템의 공통된 요소를 반영합니다.
// 다시 말해, 포션 종류 별로 사용 시 효과가 모두 다르므로, Use 함수를 가상 함수로 정의합니다.
public class Usable : Item
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
