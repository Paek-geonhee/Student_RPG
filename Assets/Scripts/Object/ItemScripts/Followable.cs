using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// 펫을 정의합니다.
// 펫 장착 시 펫이 가진 고유의 스텟을 플레이어에게 부여합니다.
// 추가적으로 장착된 펫은 플레이어를 따라다닙니다.

// 최대 3개의 펫이 장착될 수 있습니다.
// 펫 장착 해제 시 부여된 스텟을 모두 회수합니다.
public class Followable : Item
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
